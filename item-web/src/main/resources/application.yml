
server:
  port: 8081

dubbo:
  registry:
    protocol: zookeeper
    address: zookeeper.gmall.com:2181
  protocol:
    name: dubbo
  application:
    name: gmall-item-web

spring:
  thymeleaf:
    mode: HTML
    cache: false

